# server {
#     # listen 80;
# }

server {
    listen 80 default_server;
    server_name  localhost;
 
    # Statically serve the election handler:
    location /test/electionHandler {
        alias /root/sElect2/ElectionHandler/webapp/;
        index Index.html;
    }


}

